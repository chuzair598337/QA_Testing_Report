// Placeholder for the Supabase client — intentionally empty in Phase 1.
//
// No backend calls exist yet in this phase; this file exists only so the
// project's directory scaffolding (src/lib/) is in place for the phase
// that introduces real Supabase-backed data. Do not import a real client
// or read env vars here until that phase.
export {}
